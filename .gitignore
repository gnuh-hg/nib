# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Tauri (added in packaging phase)
src-tauri/target/

# Env (secrets — never commit; keep .env.example tracked)
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
*.local
.vite/

# GitNexus index
.gitnexus/

# Playwright E2E — artifact tạm (KHÔNG commit; evidence có chủ đích vẫn commit tại tests/flows/evidence/)
test-results/
playwright-report/
tests/flows/playwright/.cache
