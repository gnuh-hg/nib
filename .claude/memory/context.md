# Memory — context (trạng thái hiện tại)

> Trạng thái phase/workstream đang chạy, quyết định gần đây, việc tiếp theo, kết quả smoke-test.
> Mọi vai đọc file này đầu task. LUÔN append (`## YYYY-MM-DD HH:MM — slug`), không overwrite. Dùng 10 entry mới nhất.

---

## 2026-06-25 — free-caret-v2 RESTART (đường gõ xây lại từ zero) + model LOCKED qua AskUserQuestion
- **Bối cảnh:** lần row-based trước (RowView/caretNav/ghostCaret/mathInline/rowMeta + plan/free-caret-rebuild/) đã bị **WIPE 2026-06-25** sau bug saga (ghost-park IME, trailing-space litter, focus-steal). Schema giờ TỐI THIỂU: `doc→paragraph(inline*)→text` (NibDocument.ts). App shell + Yjs (y-prosemirror) CÒN NGUYÊN. Comment trong NibDocument.ts ghi rõ "wiped... build fresh from zero".
- **Research (researcher, Task #2, gate PASS):** xác nhận ProseMirror dùng **integer linear offset** (KHÔNG float, KHÔNG grid 2D); pixel x/y do browser layout (`posAtCoords`/`coordsAtPos`) → né được cả 2 ràng buộc user. Spectrum 4 cách fill ảo (literal space / indent-meta / hybrid / virtual-caret-IntelliJ). Bài học row-based: GIỮ geometry consts + coordsAtPos/posAtCoords pattern + Y.Map side-channel + safeEmptyDocPlugin; BỎ ghost-park materialize + synth-trailing-space + rowMeta-indent.
- **[USER CHỐT qua AskUserQuestion 2026-06-25] — MODEL LOCKED = "virtual-space free-caret" (approach d, IntelliJ-style):**
  - Dọc: tự do (click dòng bất kỳ trên trang).
  - Ngang: **cột tuỳ ý** — caret đặt đúng chỗ click; **space chỉ materialize khi GÕ** (không pre-fill → không rác, copy sạch, merge tự nhiên = không phantom char giữa 2 vùng). Căn cột **GẦN ĐÚNG** (user chấp nhận lệch do proportional font + math atom — đã chọn A thay vì hybrid-C, tức KHÔNG snap-at-math).
  - Hệ quả tự giải: copy không dính space ảo; backspace/merge = xoá text thường.
  - User ràng buộc cứng: KHÔNG float coordinates ("rất khó code"), KHÔNG integer grid cứng (vỡ vì char width khác nhau + math).
- **Rủi ro mang sang plan (researcher R1–R5):** CHAR_W=7 chỉ xấp xỉ ASCII → reference point phải là `coordsAtPos(end_of_atom).right` không phải end_of_row; IME (`compositionend`) phải handle riêng (đừng lặp ghost-park IME bug); cần `white-space: pre-wrap` trên `.nib-pm p` để giữ multiple spaces hiển thị; MathLive inline atom (`atom:true inline:true`) có drag-conflict documented — giải trước Phase math.
- **[USER REFINE 2026-06-25] model ngang đổi thành PER-LINE FLOAT-SEGMENT:** dòng i = a[i] (nguyên). NGANG = float trong dòng: mỗi dòng = list đoạn s[j]={pos,len} sort theo pos. Add char rộng x: nếu end_j+x<pos_{j+1} không merge; else merge new_len=len_j+len_{j+1}+x. Arrow up/down đổi i; left/right trong đoạn=thường, ra gap=dịch space-width. Delete kề gap=chỉnh pos ±space-width. Tab=4×space-width tạo đoạn mới (configurable). Số dòng động (tương lai setting dòng/trang; chưa có trang). User đảo "no-float" cũ: float CHỈ trong phạm vi 1 dòng.
- **FEASIBILITY VERDICT (researcher Task #4, gate PASS) — Ở LẠI ProseMirror, KHÔNG đụng [LOCKED]:**
  - **Path A** (NodeView nhiều positioned span/dòng) = BẤT KHẢ THI (PM 1 contentDOM; transform phá selection+caret).
  - **Path B "SPACER-ATOM" = LỜI GIẢI:** schema `paragraph: (spacer-atom | text)*`. spacer-atom = inline LEAF atom attr {id static}, **width(float px) lưu Y.Map side-channel keyed id** (CC-1 safe, KHÔNG node attr mutate). Đoạn text = PM text node thường. **pos/len KHÔNG lưu — đo runtime `coordsAtPos`** (browser đo chính xác proportional font + math). **Merge tự nhiên:** width→0 → PM xoá atom → 2 đoạn dính (khỏi code merge tay).
  - **Path C** (bỏ PM) = vi phạm [LOCKED], chỉ nếu B chết.
- **Quyết định LOCKED phiên này:** merge = **left-anchor** (new_pos=s[j].pos), không center (tất định, khớp gõ trái→phải). space_width/tab/char-width = **đo runtime** (canvas.measureText / coordsAtPos), KHÔNG dùng CHAR_W=7. Tab=4×space_width configurable. **IME/gõ tại gap = "materialize-on-input"** (gõ phím|IME → spaces+char thành thật ngay; KHÔNG park PM-selection ở pos vô lệ — đây là fix gốc con bug ghost-park-ime làm sập row-based cũ). Pagination dọc = presentation tách rời (1 coupling: đoạn phải white-space:nowrap). line_len = paper width 664px.
- **Rủi ro mang sang plan/architect (researcher R1–R4):** (R1) **Y.UndoManager phải track `[xmlFragment, spacerWidthMap]`** — hiện YjsSync chỉ pass xmlFragment → undo lệch width nếu không sửa. (R2) post-render measure→Y.Map update cần guard `new!==old` + rAF tránh loop. (R3) IME trong gap cần materialize-before-composition. (R4) coordsAtPos chưa chính xác trước DOM paint → đo trong `view.updateState`/rAF, KHÔNG trong appendTransaction.
- **Câu hỏi gác lại (không chặn nền):** Q3 overflow đoạn/math rộng hơn line_len (cắt/cuộn/wrap) → quyết ở phase math. Q1 arrow-gap & backspace-incremental = HOW cho architect (theo spec user space-width steps).
- **Tiếp:** planner sinh ROADMAP (multi-phase, Path B spacer-atom) + Phase đầu long-plan (PLAN-GATE) — phase đầu = schema spacer-atom + click→virtual-caret + type→materialize, gate vàng "click bất kỳ trên dòng→gõ→text đúng x". Team `free-caret`: researcher idle. Build THẲNG trên main.

### free-caret-v2 PHASE A ✅ HOÀN TẤT (2026-06-25) — gate vàng đóng, model chứng minh chạy thật
- Chain đầy đủ: researcher (2 vòng) → planner (ROADMAP 5 phase + phase-a long-plan) → architect (ARCHITECTURE.md HOW, khoá cơ chế virtual-caret↔PM-selection) → editor (A.1/A.2/A.3 + 1 bug-fix) → tester (Playwright). Lead gate độc lập + verify mỗi session.
- **Plan artifact:** `plan/free-caret-v2/ROADMAP.md` (5 phase A-E) + `plan/free-caret-v2/phase-a-schema-caret/{PLAN.md,CHECKPOINT.md,ARCHITECTURE.md}`.
- **Kết quả A.1/A.2/A.3:** SpacerAtom extension (inline leaf, width Y.Map "nib-spacer-widths" keyed id) + NibParagraph content `(spacer_atom|text)*` + virtualCaret.ts (plugin E1-safe `apply=getMeta??prev` chống remote-tx clobber; Decoration.widget side:1; handleClick **return false KHÔNG setSelection thủ công** = fix gốc vs ghost-park cũ) + materializeInput.ts (materialize 1-tx; compositionstart materializeGap+return false cho IME).
- **BUG quan trọng tester bắt (unit miss):** materialize gọi lại `coordsAtPos(lineDocPos)` → vcaret widget render tại pos đó → trả x widget → gap≈0 → spacer không tạo → text sai x. **FIX Option A:** đo `textRightClient` lúc click, lưu state, materialize tính `gap=virtualXClient−textRightClient` (regression guard mock coordsAtPos throw).
- **Evidence cuối:** tsc 0 · vitest **84/84** · build 0 · **Playwright `free-caret-v2-phase-a.spec.ts` 12/12 exit 0** (click x→gõ→text đúng x sai số 0, spacer width đúng, theme/i18n/device/E2/E6 PASS). evidence `tests/flows/evidence/free-caret-v2-phase-a/`.
- **CÒN TREO — USER SMOKE:** IME tiếng Việt tại gap (case 10 N/A — Playwright headless không mô phỏng compositionstart; robustness compositionupdate/end = Phase C). Lệnh: `npm run dev` :1420 → click vùng trống → gõ telex tiếng Việt → ký tự vào đúng x, không crash.
- **Tiếp:** Phase B (`plan/free-caret-v2/phase-b-nav-edit/`) — arrow nav 2D + backspace gap-shrink + Tab spacer. Cần planner sinh long-plan Phase B.

### free-caret-v2 — BUG user smoke phát hiện + lỗ hổng scoping (2026-06-25, PAUSED chờ user "fix team")
- **BUG (user smoke tay, Playwright miss):** trên 1 dòng đã có đoạn `a[i]` ở pos, gõ chèn vào vị trí **< a[i].pos** (bên trái đoạn) → a[i] **bị đẩy phải** thay vì đứng yên. Nguyên nhân: materialize chỉ `tr.insert` → PM text-flow đẩy nội dung phải sang; CHƯA co spacer kế tiếp đúng bề rộng chèn để neo đoạn phải (và merge khi spacer→0). = chính **luật add-char/merge gốc của user** (end_j+x≥pos_{j+1}→merge).
- **Phân loại:** FIX = merge-machinery (phía insert) → thuộc **Phase B** (ROADMAP Phase B = backspace shrink+merge; cần GỘP cả insert-side). KHÔNG phải regression code Phase A.
- **Lỗ hổng team/process (lead tự nhận, để "fix team"):** (1) **gate vàng Phase A đặt QUÁ HẸP** — chỉ test "gõ vùng trống không có gì bên phải", thiếu ca multi-segment (gõ bên trái/giữa đoạn đã có). (2) flow `free-caret-v2-phase-a.flow.md` thiếu nhóm ca multi-segment → tester miss hợp lệ theo flow nhưng flow thiếu. (3) plan UNDER-SCOPE luật add-char/merge (linh hồn model) — bị xé insert(A)/delete(B), phía insert-có-neighbor rơi khe. → Khi làm Phase B: hiện thực displace/merge CẢ insert+delete như 1 khối, + flow test bắt buộc có ca insert trái/giữa/giữa-2-đoạn-sát.
- **TRẠNG THÁI:** PAUSED. User tự fix team trước rồi mới tiếp. Team free-caret đã shutdown (editor/planner/architect/tester). Dev server :1420 còn chạy (background bq4zkmz2y).

## 2026-06-22 — Phase D (UI/Polish, scope RÚT GỌN) HOÀN TẤT frontend — accounts-cloud-sync
- Chain rút gọn: researcher → planner-light → editor-frontend (lead drive TaskList loop, gate evidence độc lập). KHÔNG full chain vì user HOÃN sync-badge — phần duy nhất cần design.
- **[USER CHỐT phiên này, qua AskUserQuestion]:** (1) Scope = **Full Phase D** ban đầu, NHƯNG sau đó (2) **sync-status badge = HOÃN ('làm sau')** → bỏ khỏi phase này; (3) AccountSection displayName/email = **GIỮ localStorage-only** (KHÔNG sync Supabase user metadata). → Phase D thực thi còn 3 deliverable: Sign Out + logout-safety + session-expired notice.
- **Quyết định an toàn dữ liệu (lead flag → planner LOCK):** ROADMAP Phase D done-criteria ghi "logout → clear Yjs doc local" — NHƯNG cloud đang PARKED local-only (Phase C) → IndexedDB là bản sao DUY NHẤT → xóa = MẤT tài liệu. → **KHÔNG destructive clear.** Thay bằng **per-user IDB namespacing**: `idbStoreName(docId,userId)` = signed-in `nib-ydoc-u-${sanitize(userId)}__${docId}`, guest/'local'/null → legacy `nib-ydoc-${docId}` (backward-compat, doc cũ không mất). Sign-out chỉ đóng handle, KHÔNG `deleteDatabase`.
- **Kết quả (editor-frontend Task #3, lead verify độc lập):**
  - **Phase 1 (Sign Out):** nút "Đăng xuất" trong AccountSection (chỉ khi profile≠null, 44px, hover→--error) → `signOutIntentional()` + đóng SettingsOverlay (mở rộng SettingsContext +onClose); hint `settings.account.local_hint` (--text-muted). +2 i18n key.
  - **Phase 2 (IDB isolation):** pure `idbStoreName` trong `src/lib/yPersistence.ts` (+test) ; YjsProvider truyền userId. gitnexus impact `createIndexeddbPersistence`=HIGH nhưng 1 call-site + param optional backward-compat → contained.
  - **Phase 3 (session-expired):** module-flag intentional-vs-expired; `useSessionExpiredNotice` (onAuthStateChange SIGNED_OUT+flag=false→expired) → banner AppShell (fixed bottom z-70 --warning) "Phiên hết hạn" → mở LoginModal/dismiss. AccountChip+AccountSection đều qua `signOutIntentional`. +1 i18n key auth.session_expired.
- **Evidence (lead re-run độc lập):** tsc 0 · `npm run build` ✓ · vitest **90/90** (baseline 87, +3 idbStoreName) · `grep deleteDatabase src/`=0 · i18n parity 195=195 · 0 hex rời. editor cập nhật design-library components.md §3 (+3 class AccountChip, logic-only không sync snippet — theo rule [[design-library-sync]]).
- **CÒN TREO — chỉ USER smoke** (Chrome ext không connect agent context; phần auth cần `.env.local` Supabase + login thật): `npm run dev` :1420 — (a) login→Settings→Tài khoản: hint "Được lưu trên máy này" + nút Đăng xuất → bấm → overlay đóng + signed-out, KHÔNG banner; (b) logout qua AccountChip dock tương tự; (c) gõ block→logout→login lại cùng user → block CÒN (per-user IDB); (d) 2 user khác máy → doc không lẫn; (e) mô phỏng token expired → banner đáy "Phiên hết hạn" → Đăng nhập mở LoginModal / X dismiss; (f) light+dark+VN/EN không vỡ ở 1024px; (g) vòng lõi gõ→Tính→result không crash sau sửa YjsProvider.
- **CÒN LẠI Phase D (HOÃN):** sync-status badge (online/syncing/offline/error) — user 'làm sau'; code SyncStatus + useYjs().syncStatus đã sẵn, chỉ thiếu UI. Khi làm → cần design present placement (TopStrip vs dot AccountChip).
- Team phase-d-ui shutdown sau entry này.

---


## 2026-06-15 — dock-v2 Phase 3 DONE: drag-reposition + overflow-aware expand/flyout
- User thêm 2 yêu cầu sau Phase 1–2: (1) dock KÉO THẢ đổi vị trí (cả mở rộng lẫn thu gọn); (2) khi dock ở thấp, expand/flyout overflow đáy → phải lật LÊN. Planner thêm Phase 3 vào plan/dock-v2/PLAN.md (2 session); editor-frontend implement; lead verify độc lập (tsc 0 / build ✓ / vitest 50/50 / 0 hex rời / 0 top-hardcode).
- **Session 3.1 (drag):** UnifiedDock → createPortal(document.body) + .nib-dock-anchor position:fixed. State pos{x,y} persist localStorage `nib-dock-pos`; dockState.ts thêm DOCK_POS_KEY/defaultPos/clampPos/parsePos (+5 test). Drag qua setPointerCapture trên DragHandle (mở rộng) + ô collapsed 56×56; tap-vs-drag threshold <4px; window.resize re-clamp; [data-dragging] tắt transition. EditorContext KHÔNG vỡ (portal chỉ đổi DOM mount).
- **Session 3.2 (overflow flip):** doExpand() chọn expandDir up/down theo spaceBelow=innerHeight-pos.y vs DOCK_EXPANDED_H(460) TRƯỚC animation (data-expand-up → CSS bottom:0;top:auto). Flyout: xóa top hardcode CSS, tính JS tại tog(k) qua btnRefs+expandedRef getBoundingClientRect → flyoutTop() fit→xuống / else lật lên clamp≥0; style prop xuống 6 flyout. Pure helpers expandDirection/flyoutTop (+3 test). prefers-reduced-motion giữ.
- **Toàn bộ plan dock-v2 HOÀN THÀNH 5 session** (1.1 scaffold / 1.2 state machine / 2.1 wire+xóa cũ / 3.1 drag / 3.2 overflow). vitest 50/50.
- **CÒN TREO (chỉ user smoke được — Chrome extension không connect ở mọi context agent):** click-through thật. User cần `npm run dev` (:1420) kiểm: kéo dock 2 trạng thái + persist reload; dock đáy→expand mở lên; flyout nút thấp→lật lên; vòng lõi gõ→Tính→result inline; Convert; B/I/U/S.
- Team dock-v2 đã shutdown + TeamDelete sau entry này.

## 2026-06-15 — tauri-shell DONE: vỏ Tauri 2 (app desktop native) — team tauri-shell
- User yêu cầu dựng vỏ Tauri 2 để Nib chạy thành cửa sổ desktop native (không qua browser). Quy trình: long-plan trước → implement → gate evidence. Team `tauri-shell` (chỉ `glue-packaging`), lead drive TaskList loop.
- Môi trường (lead đo): Ubuntu 26.04 · rustc/cargo 1.96.0 · Node v24.15.0 · webkit2gtk-4.1 2.52.3 + javascriptcoregtk-4.1 + libsoup-3.0 + gtk+-3.0 ĐỀU CÓ (KHÔNG cần apt install) · display Wayland :0 GNOME.
- Long-plan: `plan/tauri-shell/PLAN.md` + `CHECKPOINT.md` (2 session). Mọi gate PASS, lead verify độc lập.
- **Kết quả:** `src-tauri/` (Cargo.toml + build.rs + src/lib.rs + src/main.rs + tauri.conf.json + icons/). `cargo build` exit 0 (lead re-run 2.6s cached). package.json: `@tauri-apps/api@^2` (dep) + `@tauri-apps/cli@^2` (devDep) + script `"tauri":"tauri"`. tauri.conf.json: identifier com.nib.app, devUrl :1420, beforeDevCommand `npm run dev`, beforeBuildCommand `npm run build`, frontendDist ../dist, csp:null (dev), minWidth 820, **externalBin:[] = slot chừa Python SymPy sidecar (CHƯA wire)** + TODO comment trong lib.rs/Cargo.toml.
- **Session 2 (`npm run tauri dev`):** PASS ở mức process — Vite ready :1420, no panic, 0 error, `target/debug/nib` + WebKitWebProcess RSS 415MB (render React+MathLive+KaTeX+TipTap thật) + ESTAB TCP→:1420, kill sạch không orphan. **Visual UI (canvas+dock pixels) = CÒN TREO user smoke** — KHÔNG có screenshot tool nào trên máy (gnome-screenshot/grim/scrot/import/ffmpeg đều absent; gdbus GNOME Shell screenshot AccessDenied) ở cả agent lẫn lead context.
- **Bài học (mistakes.md):** `tauri-icon-must-be-rgba` — PNG icon cho `generate_context!()` phải RGBA (color type 6), không phải RGB (type 2), validate tại compile time.
- **CÒN TREO (chỉ user smoke):** `cd /home/gnuh/Documents/project/Nib && npm run tauri dev` → xác nhận cửa sổ "Nib" mở với ruled-paper canvas + UnifiedDock teal, console 0 error. Nếu blank page → respawn glue (build đã cached, rẻ).
- Team tauri-shell shutdown + TeamDelete sau entry này.

## 2026-06-16 — sidebar-design: docs/sidebar-design.md (team 2 researcher hội tụ + synth-planner)
- User yêu cầu thiết kế lại **sidebar TRÁI** (chưa build — chỉ mock trong ảnh, không có markup trong repo). Team thiết kế (chưa build): `rs-patterns` (IA pattern Notion/OneNote/Obsidian/AppleNotes/GoodNotes/Craft) + `rs-critique` (phản biện UX) → cả 2 HỘI TỤ (không mâu thuẫn, bỏ vòng cross) → `synth` (planner) viết `docs/sidebar-design.md`. Lead gate PASS từng bước; 1 vòng FAIL→re-do (synth quên áp update bỏ-folder, lead grep phát hiện, gửi diff-fix → PASS).
- **Phát hiện research then chốt:** Nib document = canvas free-placement, KHÔNG phân trang → "page thumbnail" (ý tưởng user gửi, ảnh PDF-viewer mobile) VÔ NGHĨA — đó là lý do user thấy xấu. Không app nào outline canvas-tự-do bằng heading tuyến tính; ngành dùng minimap (GoodNotes/Figma) hoặc page/section thumbnail. "Tài liệu hiện tại" nếu chỉ là tên doc đang mở = trùng affordance với list all-docs → mode confusion.
- **[USER CHỐT qua AskUserQuestion] 3 quyết định LOCKED cho sidebar:**
  1. **BỎ section "tài liệu hiện tại"** — sidebar trái = CHỈ danh sách tài liệu; tên doc + switcher dời lên **top app bar** (kiểu Google Docs); không nav/outline/minimap trong-trang ở bản này.
  2. **"Toàn bộ tài liệu" mở rộng = bề mặt Library RIÊNG** (đề xuất OVERLAY làm mờ nền + Back, KHÔNG route thật vì Tauri single-window + giữ canvas mounted để Back khôi phục state) — KHÔNG phải trạng thái fullscreen-của-sidebar.
  3. **Library quản lý ĐẦY ĐỦ MVP.**
- **[USER CHỐT bổ sung] BỎ folder/nhóm hoàn toàn** — rail thu gọn phải là **flat title list**, folder không tối ưu cho trạng thái chỉ-title. Library management MVP còn 5: rename · xoá (có confirm modal `--error`) · duplicate · sort (modified/name/created) · search-theo-tên. **Tag = hướng tổ chức TƯƠNG LAI** thay folder (note ở §9 SAU MVP, chưa thiết kế).
- **IA 3 bề mặt** (sidebar-design.md): (a) top-bar doc switcher inline-rename + dropdown recent+"Xem tất cả" · (b) sidebar rail flat list (44px item, active = `--accent-subtle`+left-edge 2px, +New, "Mở thư viện", toggle persist `nib-sidebar-open`, responsive 4 breakpoint) · (c) Library overlay (List/Grid view, preview = text/LaTeX snippet KHÔNG canvas thumbnail). ~28 i18n key `sidebar.*`/`topbar.*`/`library.*` en+vi. Token-driven, 1 token mới đề xuất `--scrim` (derive color-mix, không hex rời).
- **4 câu hỏi mở cho architect** (chặn HOW, chưa làm): data model doc-list (localStorage/Tauri store/backend) · overlay-vs-route confirm · multi-doc state trong EditorContext (undo riêng từng doc? dock state scope theo doc hay global?) · preview snippet generation (cache lúc save vs runtime).
- **Việc tiếp theo (chưa làm):** khi user muốn build → architect xử 4 câu §10 TRƯỚC, rồi editor-frontend dựng rail + top-bar + Library overlay theo docs/sidebar-design.md. Team đã shutdown sau entry này.

## 2026-06-17 — nib-editor-rebuild DONE: re-code editor khớp design HTML (team chain đầy đủ)
- User giao: "spam agent + code lại `/home/gnuh/Downloads/Nib Editor.dc.html`" — file design do claude-design dựng + user chỉnh, là **NGUỒN CHÂN LÝ; xung đột → design HTML thắng tuyệt đối**. User báo: chỉ dock phải (UnifiedDock) đúng, còn lại sai.
- Team `nib-editor-rebuild` chain ĐẦY ĐỦ: researcher → planner → architect → editor-frontend (lead drive TaskList loop, gate evidence mỗi handoff). Plan: `plan/nib-editor-rebuild/PLAN.md` + CHECKPOINT.md (5 session).
- **[USER CHỐT 2026-06-17] chỉ hỗ trợ LANDSCAPE (ngang ≥1024px)** — SIẾT yêu cầu, BỎ portrait/sub-compact 820px (lật requirements.md §2 cũ — đã cập nhật §2). Rail đẩy layout, không cần collapse-khi-hẹp.
- **Kết quả (5 session + 1 restructure, mọi gate PASS, lead verify độc lập):**
  - S1.1 tokens.css: +3 token (`--desk`/`--sheet-shadow`/`--scrim`, giữ `--overlay` riêng) + sửa 7 token sai (`--shadow-2`/`--bg-subtle`/5 swatch) light+dark khớp design dòng 22–51.
  - S1.2 TopChrome: +nút rail-toggle (IconLayoutSidebar) + bỏ HẲN virtual-keyboard + railOpen state.
  - S1.3 Canvas/paper re-layout (rủi ro cao, gate vàng): desk wrapper (--desk) → paper 664px (--sheet-shadow + ruled) + margin-line + page-title + selection-overlay + hint-pill; geometry ref canvasRef→**paperRef** (xOffset từ PAPER-left); `.nib-editor-host` absolute→relative+min-height (RISK#1), `.nib-desk` position:static (RISK#2 offsetParent=nib-pm); STARTER xOffset 40→56. +canvasLayout.test.ts (3 risk-gate test).
  - **S1.4b RESTRUCTURE (divergence R1 editor phát hiện)**: design để HEADER full-width TRÊN, rail+canvas row dưới; code cũ TopChrome trong Canvas → sai. Architect revise → tạo **`Workspace.tsx`** (sở hữu useEditor + EditorContext.Provider + paperRef + railOpen + mọi handler); **Canvas thành presentational** 5 props; AppShell simplified; nib-body/nib-stage CSS. CRITICAL: UnifiedDock+CommandPalette giữ TRONG Provider.
  - S1.4 SidebarRail (`src/components/SidebarRail/`): width 256↔0 animate (.22s cubic-bezier), doc list 44px active edge, footer Mở thư viện. +`src/types/doc.ts` + `src/data/mockDocs.ts`.
  - S1.5 LibraryOverlay (`src/components/LibraryOverlay/` 10 file): scrim(--scrim z50)+panel(z60)+toolbar+DocCard+DocRow+DocContextMenu+DeleteModal(--error)+SortDropdown+RenameField; AppShell docs→useState + handlers mutate (new/rename/duplicate/delete/sort); nib-app position:relative+overflow:hidden. +util/relativeTime.ts. 17 key library.* en+vi.
- **Evidence cuối (lead verify):** tsc 0 · `npm run build` ✓ · vitest **53/53** · 0 hex rời ngoài tokens · i18n parity. UnifiedDock KHÔNG đụng (theo lệnh user).
- **CÒN TREO — chỉ USER smoke được** (Chrome ext không connect ở mọi agent context, như tauri-shell/dock-v2 trước): `npm run dev` :1420 (hoặc `npm run tauri dev`) kiểm: (a) header full-width trên cùng; (b) rail toggle trượt 256↔0 đẩy canvas + click doc đổi active+page-title; (c) **vòng lõi gõ block→Tính→result inline KHÔNG crash** (Provider bao Dock/Palette); (d) Library overlay: Mở thư viện→scrim+panel, grid/list, ⋯ context menu (rename inline/duplicate/delete), delete modal --error, Tài liệu mới, sort 3 option, back/scrim đóng; (e) paper 664 centered + ruled + block đặt đúng vị trí paper-relative.
- **Sự cố phối hợp ghi issues.md:** ISSUE-5 (lead skip planner — TÁI DIỄN ISSUE-3, lần 2 → ngưỡng cứng-hoá đạt) + ISSUE-6 (lead bỏ sót tmux layout §8 — đính chính ISSUE-2: `$TMUX` THẬT active 5 pane, KHÔNG phải in-process no-op; lead đã áp layout N=4 trong phiên). Cả 2 high-impact chờ user duyệt cho team-ops.
- Team shutdown + dọn sau entry này.

## 2026-06-18 13:00 — design-figma-added

- Vai mới `design-figma` thêm vào roster (9 vai): agent body `.claude/agents/design-figma.md` + skill `.claude/skills/figma-design/SKILL.md` (planKey `team::1618919057199763712`, 3 workflow, gate ≥5 điều kiện, 17 Figma MCP tool).
- Vị trí chain: song song architect khi task cần visual design Figma; handoff token spec + i18n key list → editor-frontend.
- 3 file high-impact đã soạn (`master.md` Roster 8→9 + HANDOFF CHAIN + §4 rubric + §8 trỏ skill; `playbook.md` §1 Cột A/C + 6 vai + §3 per-role + §5/§7 PASS + §8 N=6; `settings.json` +17 Figma MCP tool) — **CHỜ USER DUYỆT**.

## 2026-06-18 — nav-dock-redesign: thảo luận thiết kế + build 3 session (team chain đầy đủ)
- User yêu cầu "spam agent thảo luận" về việc **gom điều hướng vào dock + bỏ header/sidebar**. Team thiết kế (3 researcher nav-patterns/ia-reconcile/critique-risk, R1 độc lập + R2 hội tụ chéo qua lead relay) → lead tổng hợp `docs/nav-dock-design.md`. Sau đó user "làm luôn nếu được" → lead đánh giá buildable trực tiếp (không cần claude-design) → chain build: planner → editor-frontend (3 session), mọi gate PASS, lead verify độc lập.
- **[USER CHỐT qua 2 vòng AskUserQuestion] — đính chính hiểu nhầm "2 dock":**
  - Dock = **MỘT** (UnifiedDock chỉnh UI/UX), **nổi+kéo-thả như cũ**, dạng **drill-down 2 level**: NAV (Thư viện/Cài đặt/Gõ/Viết/Help + AccountChip) ↔ TOOLS (Back + tool theo mode). Gõ/Viết = **2 nút entry** thay ModeToggle (1 nguồn truth mode); Back = drill-up trong dock (KHÔNG browser history — Tauri 1 cửa sổ).
  - Bỏ header+sidebar → thay bằng **TopStrip mỏng** (tên doc + dropdown đổi doc + undo/redo + theme quick-toggle). Library overlay GIỮ.
  - Account = **avatar chip tròn đơn giản** trong dock (BỎ ý tưởng bo-góc-lõm: `corner-shape:scoop` chưa hỗ trợ trên WebKitGTK/WKWebView Tauri → SVG clip-path không đáng).
  - Lang+Theme → SettingsOverlay; **theme giữ thêm quick-toggle ở strip** (§7, default — user chưa veto).
- **Kết quả build (plan/nav-dock-redesign/, 3/3 session, vitest 56/56, tsc 0, 0 hex rời, 15 key §9 en+vi):**
  - S1.1: `dockState.ts` +DockLevel/navSelect/backToNav · `NavLevel.tsx` MỚI · UnifiedDock drill-down NAV↔TOOLS+Back · **XÓA ModeToggle.tsx** · +IconSettings/IconHelp · nút dock 40→**44px [LOCKED]**.
  - S1.2: `TopStrip.tsx` MỚI (doc-title+dropdown+inline-rename+undo/redo+theme toggle) · **XÓA TopChrome.tsx + SidebarRail/** · Workspace giữ EditorContext.Provider.
  - S1.3: `SettingsOverlay/` MỚI (lang+theme runtime+persist) · wire [Cài đặt]+[Thư viện]+AccountChip · AccountChip avatar-only (dock dọc hẹp, name qua title/aria).
- **Sự cố phối hợp: ISSUE-7 (open)** — editor-frontend spawn `mode:"plan"` (plan-approval cho S1.1 xóa ModeToggle) → sau ExitPlanMode KHÔNG về acceptEdits → prompt quyền ghi mỗi file. Lead fix tạm: S1.2/S1.3 spawn **default mode** (không plan) → hết prompt. team-ops điều tra tương tác plan-approval×permission-inherit (playbook §9/§10 + settings.json defaultMode=acceptEdits). HIGH-IMPACT → chờ user duyệt.
- **CÒN TREO — chỉ USER smoke** (Chrome ext không connect mọi agent context): `npm run dev` :1420 kiểm: NAV 5 nút+avatar → Gõ/Viết drill TOOLS+Back → NAV; strip doc-dropdown+rename+undo/redo+theme; [Cài đặt]/avatar → SettingsOverlay đổi lang/theme runtime; rail+header biến mất; vòng lõi gõ→Tính→result không crash.
- **Mục mở:** theme quick-toggle ở strip (default giữ — user xác nhận sau nếu muốn bỏ). Help nút còn placeholder (chưa có nội dung Help — session sau nếu cần).

## 2026-06-19 — settings-redesign: thiết kế lại phần Cài đặt (team chain, đang chạy)
- User yêu cầu "spam agent" thiết kế lại Settings. Flow user chốt: nghiên cứu → plan (bàn design+frontend) → design → thực hiện. Yêu cầu tối thiểu: lang en/vi · theme sáng/tối/system · cài đặt tài khoản. Nhấn MẠNH (2 lần): tương lai mở rộng nhiều nhóm hơn.
- Team `settings-redesign`: researcher → planner → design-figma → editor-frontend (KHÔNG backend-cas). Harness dùng team ngầm (không TeamCreate); tasklist flaky (reset giữa chừng) — drive qua SendMessage.
- **[USER CHỐT]:** (1) Account = **UI-ONLY local profile** (tên + avatar + email tùy chọn, localStorage; KHÔNG auth/login/backend). (2) **Avatar = initials tự sinh khi chưa set ảnh; thiết kế data/UI chừa slot `avatarImage` để DỄ nâng cấp upload ảnh sau** (upload = post-MVP, cần Tauri fs/dialog). 
- **[USER CHỐT container 2026-06-19]:** Settings = **card nổi GIỐNG LibraryOverlay** (overlay lớn centered + scrim mờ canvas + header Back←+tiêu đề, giữ canvas mounted) để NHẤT QUÁN với Thư viện. User BÁC cả "modal nhỏ 1 cột" (khó mở rộng) lẫn "2 cột sidebar trong modal hẹp" (xấu) — vì container Library RỘNG nên bố cục trong có chỗ thở. **Layout BÊN TRONG "cần nghĩ kỹ"** → planner + design-figma khám phá, present mockup cho user chọn (KHÔNG tự chốt visual).
- **[LEAD CHỐT]:** (a) ~~Layout 2 cột~~ → container Library-style (trên); internal layout do design-figma dựng 2-3 hướng. (b) Account state sống trong **`ProfileProvider` mới** (đồng pattern ThemeProvider/I18nProvider, persist localStorage `nib-profile` JSON {displayName, email, avatarInitials, avatarColor, avatarImage?}) — tránh 2 nguồn truth khi sau hiện avatar ở dock/TopStrip.
- **Research (researcher PASS) phát hiện:** SettingsOverlay hiện chỉ 2 nhóm (3 i18n key settings.*), panel 460×360 sẽ CHẬT khi thêm Account → cần layout mới. AccountChip đã xóa 2026-06-18 nhưng key `dock.nav.account` còn orphan en/vi.json. Rủi ro: clamp floating (color picker avatar) theo mistakes.md; account state chưa tồn tại.
- Việc tiếp theo: planner sinh plan artifact (WHAT, design-figma+editor-frontend review) → design-figma Figma+token+i18n key → editor-frontend build.

## 2026-06-20 — design-agent-library: thay design-figma bằng agent `design` code-native + thư viện .claude/design-library/ (Phase 1 DONE)
- User yêu cầu "spam team-ops" build agent design MỚI thay `design-figma` (Figma có limit; claude-design ngoài rời rạc). 3 quyết định LOCKED (user chốt 2026-06-19): (1) output agent = mockup HTML/CSS in-repo `docs/design-artifacts/*.html` (link tokens.css + class Nib, nguồn chân lý cho editor-frontend); (2) thư viện = catalog tham chiếu `.claude/design-library/` (KHÔNG code src/); (3) THAY THẾ HẲN design-figma (roster vẫn 9 vai, gỡ Figma MCP).
- Roadmap: `plan/design-agent-library/ROADMAP.md` (phase map) + per-phase long-plan nested. Thứ tự **1→2→4→3** (proof run TRƯỚC retire design-figma — fallback an toàn nếu proof fail). Phase 3 = HUMAN-GATE (master/playbook/settings + xóa design-figma → chờ user duyệt diff).
- **Cấu trúc plan [USER CHỐT]:** mỗi phase roadmap = 1 long-plan RIÊNG nested (`plan/design-agent-library/phase-N-*/{PLAN.md,CHECKPOINT.md}`), sinh just-in-time; KHÔNG gộp 1 PLAN.md. (planner lần đầu làm monolithic → lead bắt re-do nested.)
- **Phase 1 HOÀN THÀNH (team-ops, 3 session, lead gate độc lập mỗi session):** `.claude/design-library/` 9 file — INDEX.md (hướng dẫn tham chiếu: thứ tự đọc + quy ước copy snippet) · tokens.md (60 token khớp src/styles/tokens.css) · components.md (7 component Nib + class grep pass src/ + 3 reuse pattern) · patterns/ (3 blueprint) · snippets/ (3 HTML link tokens.css, min-width:1024, data-i18n, 0 hex rời). Gate cuối: hex toàn lib = 0, mọi class tồn tại thật trong src/.
- **CC-1 [LEAD XÁC NHẬN]:** src/styles/tokens.css FREEZE trong Phase 1 (chỉ đọc).
- **Sự cố ghi issues.md ISSUE-12 (open):** lead tự soạn ROADMAP + spawn team-ops trước planner-gate (tái diễn tinh thần ISSUE-3/5) + giao planner sai cấu trúc per-phase → user nhắc 2 lần ("sao không spam agent", "spam cả plan"). Đã sửa: dùng planner soạn plan, cấu trúc nested. + tmux layout re-apply sau planner shutdown (main-pane-width 60% — ISSUE-11).
- Việc tiếp theo: re-spawn planner → long-plan Phase 2 (`.claude/agents/design.md` + `.claude/skills/design/SKILL.md`, gỡ Figma MCP, tham chiếu design-library) → gate → team-ops build → Phase 4 proof run → Phase 3 HUMAN-GATE retire design-figma.
- **Phase 2 DONE (team-ops, 2 session, lead gate đọc-thật + grep):** `.claude/agents/design.md` (agent type `design` — tools [Read,Write,Edit,Glob,Grep,Bash,TaskGet,TaskUpdate,TaskList,SendMessage]; 0 mcp__figma/chrome; entry point INDEX.md; workflow 5 bước; output docs/design-artifacts/*.html) + `.claude/skills/design/SKILL.md` (skill `design` — 5 bước, 6 done-criteria DC-1..6 grep-checklist, gate idiom script, 0 hex). Lead BỔ SUNG Bash vào tools (PLAN thiếu — agent cần tự grep self-verify). design-figma CHƯA xóa (retire ở Phase 3 HUMAN-GATE).
- Tiếp: Phase 4 proof run = agent `design` dựng `docs/design-artifacts/settings-overlay.html` (CC-1 chốt màn Settings) → gate 6 DC → rồi Phase 3 retire (chờ user duyệt diff high-impact).

## 2026-06-20 — design-library fidelity fix (ISSUE-13/14) + yêu cầu đồng bộ snippet↔src [USER CHỐT]
- User phát hiện 3 snippet `.claude/design-library/snippets/*.html` RENDER LỆCH app thật (skeleton xấp xỉ, class bịa `.nib-demo*`, chỉ link tokens.css). Gốc kép: (a) ISSUE-13 gate Phase 1 grep-only không kiểm fidelity/visual; (b) ISSUE-14 plan gộp cả 3 snippet/1 session → context quá tải → đi tắt. Cả 2 đã fixed.
- **Cơ chế VISUAL GATE thật (lead làm được):** `python3 -m http.server 8081` ở repo root → mở `http://localhost:8081/.claude/design-library/snippets/X.html` (link CSS component thật resolve được) + app thật `npm run dev` :1420 → chrome MCP screenshot ĐỐI CHIẾU. (browser file:// KHÔNG được — bị ép https; phải serve http.) Đây là cách gate fidelity cho mọi artifact design-library/docs/design-artifacts về sau.
- **Snippet rework (faithful-to-src):** link CSS component THẬT từ src (`../../../src/components/.../X.css`) + DOM/class THẬT trích từ `.tsx`. → style/token TỰ đồng bộ khi src đổi CSS; nhưng DOM/structure/icon là copy tĩnh → KHÔNG tự sync.
- **[USER CHỐT — yêu cầu đồng bộ tương lai]:** vì src sẽ còn sửa, các file design-library tham chiếu hình ảnh app (snippets/, components.md) phải được **`editor-frontend` cập nhật khi sửa UI/UX** component tương ứng (điều kiện: thay đổi là UI/UX, không phải logic thuần). Cần: (1) bảng MAPPING src-component ↔ design-library file (trong INDEX/components.md); (2) rule trong `editor-frontend.md`: sửa UI/UX component có snippet → cập nhật snippet/components.md cùng task + re-verify fidelity. → Task #9 wire (team-ops, sau Task #8 icon polish).

## 2026-06-20 — design-agent-library HOÀN TẤT TOÀN BỘ 4 PHASE (roster: design-figma → design)
- **Roadmap design-agent-library DONE** (thứ tự 1→2→4→3, mọi gate PASS, lead verify độc lập + visual localhost):
  - **Phase 1** — `.claude/design-library/` (INDEX/tokens/components.md/patterns/snippets, 9 file). Snippet rework faithful-to-src (link CSS component thật + DOM/class/icon thật từ icons.tsx), visual-gated trên localhost so app thật. ISSUE-13 (gate grep-only thiếu visual) + ISSUE-14 (plan gộp file nặng/1 session) fixed.
  - **Phase 2** — `.claude/agents/design.md` (code-native, tools có Bash, 0 Figma/chrome) + `.claude/skills/design/SKILL.md` (8 DC, motion-intent, gsap-* ref). ISSUE-15 (thiếu gsap ref) fixed.
  - **Phase 4** — proof run `docs/design-artifacts/settings-overlay.html` (agent design dựng) — **visual PASS**: khớp Settings overlay thật cả light+dark (4 tab, icon, avatar, badge, token-driven CSS thật) + motion-intent handoff.
  - **Phase 3 [USER DUYỆT]** — retire design-figma: `git rm` design-figma.md + figma-design/SKILL.md (revert: `git checkout 2c1508f`); master.md (4) + playbook.md (5) đổi design-figma→design; settings.json gỡ 17 perm mcp__claude_ai_Figma__* (allow còn 12); team-fix SKILL §7 row figma → RETIRED. Gate: grep design-figma sống=0 (chỉ còn câu lịch sử), Figma MCP=0, JSON valid, roster 9 vai có `design`.
- **Cơ chế bền vững đã wire:** (a) snippet link CSS component thật → style tự sync; (b) bảng MAPPING src↔design-library + rule editor-frontend.md cập nhật khi sửa UI/UX; (c) visual-gate qua `python3 -m http.server 8081` (repo root) + chrome MCP (lead foreground); (d) plan-long/planner session-granularity rule (1 heavy file/session). Xem memory personal [[design-library-sync]].
- Roster note: vai visual design GIỜ là `design` (code-native), KHÔNG còn `design-figma`/Figma. Mọi task UI/visual → spawn `design`.

## 2026-06-20 — maintenance HOÀN TẤT 3 phase (team-ops, roster cleanup + workflow hardening)
- User giao "spam team-ops, nhiệm vụ bảo trì" (4 việc: compact docs/plan · audit agent/skill called-or-not + lý do · upgrade agent/skill · workflow lead hay skip plan/design/research). Lead drive chain researcher(audit)→planner(roadmap+phase-A long-plan)→team-ops(exec) — KHÔNG tự soạn plan (tránh ISSUE-12). Roadmap: `plan/maintenance/`.
- **Audit (researcher PASS):** 9 agent (backend-cas/handwriting dormant HỢP LỆ chờ backend/license; còn lại đã dùng). 24 skill: 8 gsap-* over-provisioned NHƯNG [USER CHỐT] GIỮ (sẽ dùng fix animation sau); latex-sympy/handwriting-myscript dormant hợp lệ. Phát hiện: planner.md thiếu master/playbook trong reading-list; "8 vai" stale; project name "note-ch" stale khắp .claude/ (nay = Nib); 3 CHECKPOINT design-agent-library mismatch; docs/design.md §2.3/4.x stale.
- **[USER CHỐT 4 quyết định]:** GSAP giữ · plan-done+docs-stale ARCHIVE (`_archived/`) · `plan/ROADMAP.md` phẳng XÓA · workflow làm cả 4 (ISSUE-7/10/11/12).
- **Phase A (team-ops, 6 session, lead verify độc lập):** archive 7 plan dir → `plan/_archived/` + 6 docs stale → `docs/_archived/` (git mv giữ history); xóa `plan/ROADMAP.md`; annotate `docs/design.md` SUPERSEDED banner+4 note; trim `context.md` 22→10 + `patterns.md` 20→10 (tạo `*-archive.md`); issues.md status header; master.md §8 ref-path. plan/ giờ chỉ còn _archived/maintenance/settings-redesign.
- **Phase B (low-impact):** planner.md reading-list +master+playbook; "8 vai"→"9 vai" (7 agent); rename note-ch→Nib (9 agent + 10 skill); GSAP keep-note INDEX.md; phase-a CHECKPOINT→100%.
- **Phase C (HIGH-IMPACT, [USER DUYỆT toàn bộ diff]):** master §1 + playbook §11 — cứng-hoá: soạn plan = planner, dựng mockup = design, lead KHÔNG tự làm (ISSUE-10/12). playbook §8/§2 tmux: `main-pane-width 60%` trước select-layout + re-apply sau shutdown + N-dynamic guard (ISSUE-11, lặp ≥3). playbook §10 ISSUE-7 workaround (đừng spawn mode:plan cho session ghi; brief-level STOP thay thế) + `findings-issue7.md`. note-ch→Nib + 8→9vai trong master/playbook. Renumber Teammate anti-patterns 11-18. ISSUE-7/10/11/12 → fixed. Issue queue giờ Open=0.
- **Lead fix bootstrap:** thêm `Bash` vào team-ops.md tools (trước thiếu — body dặn chạy python3 json.load mà không có Bash; latent bug). team-ops giờ làm được file-ops/grep/json-validate.
- **Bài học process:** message race teammate↔lead lặp lại (team-ops/planner báo "task done" stale, không thấy task mới) → lead phải resend nudge "TaskGet(N) trực tiếp". Không chặn nhưng tốn 1 round-trip mỗi handoff.

## 2026-06-21 — accounts-cloud-sync: workstream MỚI khởi động (user chốt hướng C) + plan PASS
- User hỏi "app có tài khoản, đổi máy phải thấy lại dữ liệu" → chốt **hướng C = account thật + cloud sync CẢ tài liệu toán**. **ĐẢO [LOCKED] cũ** "Account = UI-ONLY local, KHÔNG auth/backend" (context.md 2026-06-19) — thay đổi spec có chủ đích.
- **Quyết định LOCKED session này (user chốt):**
  1. Sync engine = **Yjs (CRDT)**, KHÔNG LWW (offline-first + merge an toàn, mất block toán nghiêm trọng; tái dùng y-prosemirror).
  2. Kết quả CAS (exact/approx LaTeX) = **phần của block/doc**, persist+sync CÙNG block (user sửa false-dichotomy của lead: kết quả không tách rời doc).
  3. Backend = **Supabase (auth+Postgres) + Hocuspocus (MIT self-host Yjs WS) trên Render** — fit research; loại Liveblocks ($99/mo+lock-in) + y-supabase (not production-ready).
  4. **Render + Supabase = FREE TIER** (user chốt 2026-06-21). Hệ quả: Render ngủ 15ph→cold-start sync (offline-first che); Supabase 500MB+pause-1-tuần → CC-3 nén snapshot quan trọng. = CC-2 resolved.
- **Rủi ro then chốt CC-1:** y-prosemirror **không sync node attributes đúng** (bug đã biết) — xOffset/lineIndex/kết quả của block là node attrs → architect Phase B phải chốt HOW (node attr vs Y.Map riêng) trước khi implement, không thì block dồn x=0 vỡ layout.
- **Plan PASS (planner, gate lead):** `plan/accounts-cloud-sync/ROADMAP.md` (4 phase A-Auth/B-Sync/C-Deploy/D-UI) + `phase-a-auth/{PLAN.md,CHECKPOINT.md}` (3 session). CC-4 (keyring plugin Tauri) chặn A.2; A.1 chạy ngay.
- **Sự cố ISSUE-16 (open, mới 1 lần):** lead spawn researcher KHÔNG đọc hard-gate §13 (master+playbook+context) + sai recipe (Agent nền brief-in-prompt thay TaskCreate). User nhắc. Lead khắc phục: đọc đủ 3 file, ghi issue, tiếp chain đúng recipe từ planner. Research output vẫn PASS nên không re-run.
- **Đang làm:** build Phase A.1 (editor-frontend) — supabase client singleton + auth module + .env.example. CHƯA tới bước Claude Chrome setup web (Phase C human gate).

## 2026-06-21 — Phase A (Auth) HOÀN TẤT frontend — accounts-cloud-sync
- Toàn bộ Phase A done, lead verify độc lập + visual gate app thật mỗi session:
  - **A.1** (editor): `src/lib/supabase.ts` (client singleton) + `src/lib/auth.ts` (signIn/signOut/getSession/onAuthStateChange + sau thêm signUpWithEmail) + `.env.example` + mock supabase trong test. vitest 62.
  - **A.2-Rust** (glue): `src-tauri/` keyring crate "4" + 3 command `save_token/load_token/clear_token` (invoke_handler), OS keychain (secret-service/Keychain/Credential Manager), reject khi daemon down. cargo build 0. CC-4 chốt = OS keychain + fallback.
  - **A.2-JS** (editor): `src/lib/tokenStore.ts` (saveToken/loadToken/clearToken + initTokenSync) bọc 3 invoke + fallback localStorage khi keychain reject. vitest 72.
  - **A.3-logic** (editor): `src/providers/ProfileProvider/` migrate → Supabase user (deriveProfile, profile nullable=signed-out, session trong context). vitest 76.
  - **A.3-design** (design): `docs/design-artifacts/login-modal.html` mockup login+signup (8/8 DC), user duyệt. Bản fancy split-layout/illustration để Phase D (memory [[future-fancy-auth-ui]]).
  - **A.3-build** (editor): `src/components/LoginModal/` (login/signup toggle, GSAP motion+reduced-motion) + `AccountChip` (signed-out→mở modal, signed-in→avatar+menu Đăng xuất) + IconAlertCircle/IconLogOut + 29 i18n key (login/signup/account) en/vi parity 192=192. vitest 82.
- **Visual gate (lead foreground chrome, :1420) bắt 1 bug**: LoginModal mở thì UnifiedDock (z:30, portal body) đè lên → editor fix z scrim/panel 90/100 (.nib-app không phải stacking context). Re-verify: modal render sạch trên dock, i18n VN đúng. (Ảnh mid-fade gây hiểu nhầm 1 nhịp — đã xác nhận elementFromPoint=modal + panel opaque.)
- **CÒN TREO — USER smoke**: login thật cần `.env.local` Supabase config (chưa có project — Phase C human gate); theme/lang runtime; escape/scrim đóng; reduced-motion; vòng lõi editor không vỡ.
- **Tiếp**: Phase B (Sync Engine Yjs) — BẮT ĐẦU bằng architect giải CC-1 (y-prosemirror node-attrs: xOffset/lineIndex/kết quả block) TRƯỚC khi implement. Index gitnexus đã re-index tươi.
- Editor/glue/design đã shutdown sau Phase A.

## 2026-06-23 — free-caret-rebuild KHỞI ĐỘNG (user chốt Hướng C) + Phase A Architect DONE
- User: "hoàn thiện tính năng dock, bắt đầu từ gõ". Hiện tại click vào giấy → tạo NGAY 1 math-block teal (Workspace.handlePointerDown→createBlock). User muốn **con trỏ đặt bất kỳ đâu trên dòng + gõ = INSERT** (mental model: tờ giấy fill sẵn khoảng-trắng). Phụ: bỏ 2 nút toolbar MathLive (☰ menu + ⌨ virtual-keyboard).
- **[USER CHỐT qua AskUserQuestion, 2 vòng — LOCKED]:** chọn **Hướng C = FREE-CARET DOCUMENT THẬT (rebuild)** KHI ĐÃ BIẾT RÕ CHI PHÍ (lead trình rõ B vs C). Nghĩa: rebuild document model sang **ROW-BASED** — mỗi dòng = chuỗi editable liên tục, math = inline atom, caret văn bản thật đi xuyên trang (xuyên qua cả math atom), gõ = insert không đè. C1 chốt: Left/Right arrow ĐI VÀO atom kề = CÓ. Đây ĐẢO mô hình block-free-placement hiện tại + adapt lại sync Phase B. Rủi ro cao, nhiều phase.
- Team `caret-input` (chain đầy đủ, lead drive TaskList loop, gate evidence): researcher (free-caret feasibility, R1–R6) → planner (ROADMAP 6 phase + Phase A long-plan) → architect (opus, §1–§7 ARCHITECTURE.md). Researcher shutdown sau Task #1; planner+architect giữ.
- **ROADMAP** `plan/free-caret-rebuild/ROADMAP.md` (6 phase): A Architect → B Schema+Migration → C Text Engine+Cursor → D Inline Math+Dual-Caret (+bỏ 2 nút) → E CAS+Vòng-lõi (gate vàng "gõ x²→Tính→2x inline") → F Sync+Polish. 4 [LOCKED]: row-based · MathLive WYSIWYG giữ · BẢO TOÀN dữ liệu cũ (cấm deleteDatabase) · vòng lõi sống sau mỗi phase.
- **Phase A DONE — `plan/free-caret-rebuild/phase-a-architect/ARCHITECTURE.md` §1–§7 (lead gate PASS 2 session):**
  - §1 Schema: `doc(row*)` · `row{id}` · `mathInline{id}` atom inline · text+marks · result KHÔNG-phải-node (render bởi NodeView atom cuối range). **Mọi PM attr static (chỉ id) → né bug CC-1** (chỉ attr MUTATE mới dính). blockMeta reuse keyed **atom-id** (bỏ xOffset/lineIndex); NEW **rowMeta{blankBefore,indent}** keyed row-id (file mới yRowMeta.ts).
  - §2 Whitespace VIRTUAL giải R1 (không fill literal space; doc trống=0 row, encodeStateAsUpdate vài chục byte không ~180KB). blankBefore (gap dọc) + indent (offset ngang) trong rowMeta; caret-position KHÔNG persist.
  - §3 Migration an toàn: detect schemaVersion/scan nibBlock → convert vào **store mới `__v2`** (store cũ = backup, KHÔNG đụng) qua prosemirrorJSONToYDoc → verify → switch; fallback giữ cũ+doc rỗng+notice+retry; **CẤM deleteDatabase** + test matrix 4 case (spy deleteDatabase NEVER).
  - §4 Dual-caret: ghost Decoration.widget (virtual-park) + handoff caret↔MathLive thứ tự rAF-defer né bug PM-selection-clear (discuss.prosemirror.net/9021).
  - §5 Insert: PM-default insert không đè; gap giữa 2 atom backspace = xoá atom-trái (**KHÔNG merge**); §6 Arrow-nav 2D goalX qua coordsAtPos/posAtCoords; §7 Vòng-lõi: MathLive WYSIWYG giữ trong atom (row-text=literal vs atom=WYSIWYG), Tính target precedence + evalSelection multi-atom, tái dùng evalBlock/ResultView.
- **Rủi ro chuyển phase sau (ARCHITECTURE.md cuối):** (1) **WS room v2 versioning** `${userId}:${docId}:v2` — Phase B/C BẮT BUỘC tránh client v2 merge state v1 server = corrupt; (2) reconstruct latex đa-atom — Phase E; (3) spike MathLive inline setCaretPoint/position — Phase D ≤1 ngày.
- **Sự cố nhỏ (đã biết, không chặn):** TaskList harness reset giữa chừng (flaky như settings-redesign) → architect không TaskGet được Task #4 → lead drive brief A.2 **inline qua SendMessage**. Gate vẫn bằng evidence trong report.
- **TIẾN ĐỘ build (lead drive TaskList loop, verify độc lập mỗi session — build THẲNG trên main theo user chốt):**
  - **Phase B DONE (3 session)** — schema row/mathInline (attr chỉ id) + NibDocument 'row*' gỡ NibBlock; blockMeta keyed atom-id (bỏ xOffset/lineIndex) + yRowMeta + useRowMeta + MetaSyncPlugin (appendTransaction auto-init, GC lazy undo-safe); migration.ts (convert→store `__v2` riêng→verify→fallback, CẤM deleteDatabase) + room `:v2` + i18n. vitest 118/118, R1 byte<1KB, migration 12/12.
  - **Phase C DONE (3 session)** — RowView.tsx (blankBefore→margin-top, indent→padding-left geometry) + text insert; ghostCaret.ts (click-to-position via posAtCoords + ghost-park transient KHÔNG persist + materialize whitespace ảo khi gõ); caretNav.ts (arrow up/down goalX 2D + Tab→atom; left/right atom=PM default). vitest 157/157. Editor giờ CHẠY ĐƯỢC (caret đặt-bất-kỳ-đâu + click + nav 2D), CHỈ mathInline còn placeholder `[Math]`.
  - **CÒN TREO — USER SMOKE caret UX** (`npm run dev` :1420; Chrome ext không connect agent): click text/dòng-trống→caret/ghost; gõ tại ghost→tạo row đúng vị trí; arrow up/down giữ cột (goalX); Tab→atom; left/right qua atom 1 bước. (checklist đầy đủ trong report C.2+C.3.)
  - **Caveat migration:** IDB round-trip THẬT chưa test (jsdom thiếu indexedDB) — chỉ verify logic + tĩnh không-deleteDatabase; cần browser-smoke với doc cũ thật khi vòng lõi sống (Phase E).
- **Phase C BUG SAGA + CHỐT (2026-06-23→24):** user smoke báo "không hoạt động / con trỏ giả gõ sai chỗ". Lead browser-gate (foreground chrome — chỉ lead reach được, ISSUE-8) lần lượt fix: (1) `posAtCoords` KHÔNG bao giờ trả null cho click vùng trống → classifyClick dùng `coordsAtPos` so click (vertical/horizontal miss); (2) MathLive font-fail artifact `#ML__fonts-did-not-load` cướp focus offline → focusin listener + tabindex + auto-focus mount; (3) keystroke rơi vào parked-selection → materialize chuyển vào `editorProps.handleKeyDown` (return true chặn PM); + double-seed 2 starter row (StrictMode) → 4-layer guard. **GỐC RỄ chuỗi "vẫn lỗi" = STALE HMR** (lead test nhầm dev server cũ :1422; restart sạch :1420 → chạy đúng). Bài học `mistakes.md` stale-HMR-dev-server-phantom-failures: editor-core change → kill vite + restart sạch trước gate; build+unit-test miss runtime crash.
- **Verify 2 lớp PASS:** (a) lead browser-gate tay :1420 (1 starter row, click trống→ghost→gõ "hi" đúng chỗ); (b) **tester tự chạy Playwright 8/8 exit 0** (`tests/flows/playwright/free-caret.spec.ts` = regression test tái dùng, 12 screenshot, 0 JS error). → **Phase C free-caret CHỐT.**
- **Bộ máy team nâng cấp (team-ops, USER DUYỆT 2026-06-24):** (A) playbook §8 layout fix — kill zombie pane (`pane_dead=1`) trước khi đếm N + ngưỡng main-vertical N≤5; (B) **Playwright** `@playwright/test`^1.61.1 + chromium headless → tester EXECUTE browser autonomously (browser-test SKILL §0 primary, Chrome MCP secondary foreground) — giải ISSUE-8 cho tester nền; (C) **rule serialize:** tester-app-execute ⊥ editor/backend code-edit (playbook §9 + 3 agent body). ISSUE-18/19 fixed.
- **Lead note:** lead có dùng `indexedDB.deleteDatabase` xoá IDB test trên :1420 (gồm store per-user) để gate doc tươi — chỉ mock/test data (app đang rebuild), nhưng trái nguyên tắc an-toàn-dữ-liệu; lần sau hỏi user trước.
- **Tiếp:** Phase D (MathLive inline atom thật + dual-caret handoff §4 + BỎ 2 nút ☰/⌨) → Phase E (CAS/Tính, gate vàng "gõ x²→Tính→2x") → Phase F (sync 2-tab + polish). Từ Phase D, tester chạy Playwright verify mỗi phase. Team caret-input: editor + tester idle giữ máy.

## 2026-06-22 — Phase B (Sync Engine, Yjs) HOÀN TẤT frontend — accounts-cloud-sync
- Chain: architect (CC-1 + HOW) → planner (5-session long-plan) → editor (B.1-B.5). Lead verify độc lập mỗi session + visual smoke cuối.
- **CC-1 GIẢI = (a) Y.Map side-channel**: block layout/CAS fields (xOffset/lineIndex/blockState/latex/exact/approx/...) lưu trong `Y.Map "blockMeta"` keyed block-id, KHÔNG để là PM node-attrs (né bug y-prosemirror). PM node `nibBlock` chỉ giữ 3 attr structural {id,blockType,starter}. HOW đầy đủ: `plan/accounts-cloud-sync/phase-b-sync-engine/ARCHITECTURE.md`.
- **B.1** yjs core: src/lib/{yjs,yProvider,yPersistence}.ts + src/providers/YjsProvider/. Deps: yjs 13.6, @hocuspocus/provider+server ^4.3, y-prosemirror ^1.3, y-indexeddb ^9 (deviation: plan pin ^2 sai — y-indexeddb không có major 2). vitest 82.
- **B.2** block-meta: yBlockMeta.ts (Map-of-Y.Map, LWW per-key, transact) + useBlockMeta (observeDeep, DEFAULT_META R3) + useYjsStatus + BlockMetaRecord. vitest 87.
- **B.3** (HEAVY) strip NibBlock 17→3 attr + migrate NibBlockView→useBlockMeta/patchBlockMeta + blockActions(ydoc) + bỏ field NibBlockAttrs. R1 grep 0. Kéo slot `ydoc:Y.Doc|null` vào editor-context sớm. vitest 87.
- **B.4** YjsSync.ts (ySyncPlugin+yUndoPlugin+UndoManager qua storage+keyboard) + DELETE NibHistory.ts + TopStrip/CommandPalette undo qua editor.storage.YjsSync?.undoManager (optional-chaining). vitest 87.
- **B.5** (HEAVY) Workspace tách Workspace(outer, wrap YjsProvider key=docId, userId/token từ ProfileProvider) + WorkspaceEditor(inner, xmlFragment từ useYjs().ydoc → YjsSync.configure + ctx.ydoc thật). GOTCHA: ySyncPlugin bỏ qua content: → seedStarter() vào ydoc với CRDT flag docMeta.seeded (idempotent 2-tab). tsc 0, build 0, vitest 87.
- **Lead visual verify**: app load sạch, starter block render ĐÚNG qua đường Yjs (đúng vị trí + kết quả, không DEFAULT_META) → editor lõi KHÔNG vỡ.
- **GATE VÀNG 2-tab sync CÒN TREO**: cần token≠null (auth thật) để nối Hocuspocus → cần **Phase C** (Supabase project + Render deploy). Signed-out hiện = offline-only IndexedDB (persist reload, KHÔNG sync 2-tab). Chạy hocuspocus local: `node -e "const {Server}=require('@hocuspocus/server'); Server.configure({port:1234}).listen();"` + `VITE_HOCUSPOCUS_URL=ws://localhost:1234 npm run dev`.
- **Defer**: convert latex↔text bridging qua meta (TODO trong NibBlock.ts) — follow-up nếu cần.
- **Tiếp Phase C**: HUMAN GATE — user setup Supabase project (schema yjs_updates + RLS) + Render Hocuspocus service (free tier) + onAuthenticate JWT validate room `${userId}:${docId}` (R5). Đây là lúc dùng Claude Chrome vào Supabase/Render. CC-2 (free tier) + CC-3 (snapshot compaction) chốt ở Phase C.
- Editor shutdown sau B.5.

## 2026-06-23 — Vai MỚI `tester` (browser-E2E) thêm vào roster (10 vai) + skill + flow folder
- User yêu cầu build agent `tester` lái Chrome thao tác + skill bắt buộc "lên kế hoạch / flow test" (test gì / case nào / khi nào). team-ops thực thi (.claude/), lead tạo scaffold ngoài .claude/.
- **3 file .claude/ (team-ops, Task #1, lead gate đọc-thật):** `.claude/agents/tester.md` (tools 22: 6 std + 4 task + 12 chrome MCP; ranh giới CHỈ ghi tests/flows/; 2 pha PLAN background / EXECUTE foreground) + `.claude/skills/test-planning/SKILL.md` (BẮT BUỘC; 6 nhóm case + 3 req nền [LOCKED] + 5 trigger type + done-criteria "ready") + `.claude/skills/browser-test/SKILL.md` (Chrome MCP sequence + dialog-avoid + click-through fallback).
- **Scaffold flow (lead, ngoài .claude/):** `tests/flows/README.md` (Catalog) + `tests/flows/_TEMPLATE.flow.md`. Flow file = `tests/flows/<slug>.flow.md`, evidence `tests/flows/evidence/<slug>/`, status draft→ready→executed.
- **Sweep tích hợp HIGH-IMPACT [USER DUYỆT qua AskUserQuestion]:** master.md (Roster 9→10 + dòng tester + reword "no tester role"→hai lớp kiểm chứng + §4 rubric + §7 hướng dẫn lead) · playbook.md (×2 "roster 9 vai"→10 + §1/§3/§7) · 10 agent body "roster 9 vai"→"10 vai" · CLAUDE.md §13 "8 vai"→"10 vai" (lead tự sửa) · settings.json chrome → **whole-server `mcp__claude-in-chrome`** (user chọn "mọi quyền", thay 12 entry tường minh). JSON valid.
- **⚠️ ISSUE-8 áp cho tester:** Chrome extension foreground-only → tester chạy background CHỈ plan flow được; EXECUTE Chrome phải foreground (lead foreground hoặc user). Ghi rõ trong agent body + browser-test skill. tester KHÔNG thay build-verify của implementer — hai lớp bổ sung.
